Before running the script, ensure you have:

Python 3 installed on your system (Check with "python3 --version." in terminal)
VSCode as your code editor

Replace "YOURFILENAME.py" with the actual name of your script.
If pip3 install google-play-scraper doesn’t work, try pip install google-play-scraper.
Ensure Python is added to your system’s environment variables if you encounter command errors
